# Session task log — 2026-07-19

<!-- Auto-managed by /kb-spec log. Every entry is either pending [ ] or done [x].
     Do not hand-edit the checkbox syntax — use /kb-spec log done to flip state. -->

## Pending

## Done
- [x] 14:45 — Why only 4 lease fields ✓ 15:30 — EJAR financials live on page 3; now sends pages 1-3 in ONE call. Verified live with real lease: rent 40000, 10 payments × 20000 نصف سنوي, mobile, CR block, city الدمام, area 92 — all correct
- [x] 14:45 — UI: animate + smooth-scroll to extracted fields, color them ✓ 15:30 — .ai-extracted emerald tint (until user edits) + scrollIntoView to first filled field + escalated badge
- [x] 14:45 — Audit all form fields vs extractor schema ✓ 15:30 — added owner_mobile, unified_number, tenant_cr_*, city, shop_area; one lease fills lease + CR + municipal sections
- [x] 14:10 — Fix "Page Unresponsive" freeze on extraction success ✓ 14:25 — aiMotion MutationObserver removed/re-added the fields-chip on every fire → infinite self-trigger loop blocking the main thread. Made chip update idempotent in all 6 AI blades. Commit 0478c77, deployed. (Server side was fine — job #2 extracted the lease correctly.)
- [x] 13:40 — Fix 500 Internal Server Error on live app ✓ 13:50 — root-owned storage files from root SSH artisan runs; chown -R www-data:www-data storage bootstrap/cache; deploy flow updated
- [x] 13:40 — Save today's work to dev KB ✓ 13:55 — raw/2026-07-19-abd-deploy-gap-adaptive-escalation.md ingested: new source page + concepts/adaptive-model-escalation + updated gemini-model-selection; index + log appended
- [x] 12:00 — Adaptive model escalation: flash-lite first, gemini-3.5-flash (minimal→medium) for hard PDFs ✓ 13:20 — commit b595c7b, deployed + verified live (easy lease stays on flash-lite, 100% conf, no spurious escalation)
- [x] 12:00 — Token saving round 2 ✓ 13:20 — escalation reuses same prepped page (no re-raster); thinking level added to cache key; per-model pricing in ledger
- [x] 12:00 — UI improvements for AI extract UX ✓ 13:20 — 🔍 "فحص دقيق بالنموذج الأقوى" badge when the strong model rescues a hard scan
- [x] 12:00 — Research Gemini models/pricing online ✓ 12:20 — 3.5 Flash $1.50/$9, 3.1 Flash-Lite $0.25/$1.50 (6× cheaper) per benchlm.ai; flash-lite right for extraction volume, 3.5 for hard docs
- [x] 12:00 — Server speed: opcache + config/route cache + APP_DEBUG off ✓ 12:50 — opcache was on; APP_ENV=production + APP_DEBUG=false; fixed 2 duplicate route names blocking route:cache; config+route caches live; pm.max_children 5→12; login 0.15s warm
- [x] 11:05 — Optimize InteractiveDocPrep: rasterize only page 1 (pdftoppm -f 1 -l 1) ✓ 11:35 — commit 926d543, deployed
- [x] 11:05 — Restore local MySQL so full test suite runs locally ✓ 11:35 — N/A: MySQL never installed locally (no brew pkg, no Docker); tests run under SQLite :memory: instead — all green

## Done
- [x] 11:05 — End-to-end verify AI extract pipeline on live server ✓ 11:20
  - All 6 extractors OK (expense extracted real vendor/amount; shop real user PDF → lease fields 100% confidence)
  - Cache verified: repeat call cache_hit=1, hit_count=1, ~$0.0039/call logged in ai_usage_log
  - Async queue flow: pending → processing → done via live abd-queue worker
- [x] 10:15 — Investigate app slowness + PDF AI extract not working properly ✓ 10:55
  - Root cause: live server (91.230.110.187:9095) was 5 commits behind — missing async extraction (Phase 3), efficiency layer, and the PDF mimetype fix
  - Deployed: merged 007-ai-client-feedback-efficiency → main, pushed, pulled on server, 5 migrations applied, caches cleared, abd-queue restarted
  - Verified: ai-extract-async route live, no errors, QA tests green (local MySQL was down — ran under SQLite)
  - Also fixed local .env: GEMINI_DEFAULT_MODEL gemini-3.5-flash → gemini-flash-lite-latest (3.5-flash overloaded: 500/429/503)
